language: r

warnings_are_errors: true

apt_packages:
 - libcurl4-openssl-dev
 - libxml2-dev
r_binary_packages:
 - RUnit
 - testthat

r_github_packages:
 - rikenbit/meshr_release/meshr

notifications:
  emails:
    - k.t.the-answer@hotmail.co.jp
  on_failure: always